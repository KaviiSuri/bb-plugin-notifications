import { definePluginApp } from "@bb/plugin-sdk/app";

/**
 * Frontend entry point for BB Notifications.
 *
 * UI contributions will be added with the notification behavior.
 */
export default definePluginApp(() => {});

import type { BbPluginApi } from "@bb/plugin-sdk";

/**
 * Backend entry point for BB Notifications.
 *
 * Notification behavior will be added in a later task.
 */
export default function plugin(_bb: BbPluginApi): void {}
